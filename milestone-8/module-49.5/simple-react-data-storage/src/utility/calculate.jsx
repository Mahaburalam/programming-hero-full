/* eslint-disable no-unused-vars */
const Addition = (first, second) => {
    return (first + second);
}

const Multiplication = (first, second) => {
    const output = first * second;
    return (output);
}

// export default Addition;
export {Multiplication, Addition}